.venv/
__pycache__/
*.pyc
*.pyo
.env
*.db
.pytest_cache/
htmlcov/
.coverage
logs/
*.log
# Technical memory docs — contain production secrets, never commit
REPORTES/

# Trained HMM models (regenerated by the weekly training job / backfill)
# Anchored to repo root so it does NOT match app/models/ (source code).
/models/

# ── Datos pesados / binarios que no deben versionarse ──
# Git bundles
*.bundle

# Datos de mercado / exports
NINJATRADER/HOLC/
ClaudeCodeTV_*/ListaDeOperaciones/*.csv

# Documentos binarios generados
DOCS/*.pdf
**/*.docx
**/*.pptx
**/*.xlsx

# Sistema operativo
.DS_Store
Thumbs.db
desktop.ini
